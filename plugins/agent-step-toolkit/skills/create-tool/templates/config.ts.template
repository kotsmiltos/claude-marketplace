// FILE: src/tools/{{TOOL_NAME}}/config.ts
//
// Pure data, ASSEMBLED here: each action's declaration lives beside the
// executor that names its verdicts (actions/<name>/action.ts); this file only
// collects them. The runner cross-references the assembled config against the
// executors / verifiers registries at construction time.
//
// The tool description is ONE semantic sentence — the engine composes the
// mechanics (batching, result shape, the gate handshake, the turn protocol)
// into the action descriptions and the protocol prompt fragment
// (`composeProtocolPrompt`); restating them here duplicates engine bytes.

import type { State } from "../../state.js";
import { defineConfig } from "../../agent-step/index.js";
import type { ActionName, PrereqName } from "./names.js";

import { {{ACTION_1_CAMEL}}Action } from "./actions/{{ACTION_1}}/action.js";
import { {{ACTION_2_CAMEL}}Action } from "./actions/{{ACTION_2}}/action.js";
import { {{ACTION_3_CAMEL}}Action } from "./actions/{{ACTION_3}}/action.js";

export const {{TOOL_NAME}}Config = defineConfig<ActionName, PrereqName, State>({
  tool: {
    name: "{{TOOL_NAME}}_agent_step",
    description:
      "Run ordered {{TOOL_NAME}} workflow steps. Later steps see earlier state updates; execution stops at the first non-ok result.",
  },
  actions: {
    {{ACTION_1}}: {{ACTION_1_CAMEL}}Action,
    {{ACTION_2}}: {{ACTION_2_CAMEL}}Action,
    {{ACTION_3}}: {{ACTION_3_CAMEL}}Action,
  },
});
