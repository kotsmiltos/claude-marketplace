# Tool Plan: {{TOOL_NAME}}

> Generated by /create-tool. Review and confirm before files are written.

## Summary
{{ONE_SENTENCE_DOMAIN_SUMMARY}}

## Actions

| name | params | prereqs | mutation? | lifecycle opts | summary |
|------|--------|---------|-----------|----------------|---------|
| {{action_1}} | {{params}} | {{prereqs}} | no | — | {{summary}} |
| {{action_2}} | {{params}} | {{prereqs}} | no | — | {{summary}} |

> For each **read** action, note its result size. A large/unbounded list (search / browse / history) is a **paginated read**: declare `pageable` on the action (with a `z.object` params schema). The runner injects `page`/`pageSize`, returns one page, and caches the full set in the library-managed `pagedRead` slot — no per-tool state slot or cache helper needed. Mark such actions below so the plan reflects it.
| {{action_3}} | {{params}} | {{prereqs}} | yes — soleOnExecute, requiresConfirmation { maxAttempts: 3 } | — | {{summary}} |

If the tool has a multi-turn flow (OTP-gated activation, double-entry PIN, etc.), expand the **lifecycle opts** column for each action using these tokens:

| token | meaning |
|-------|---------|
| `startsFlow=X` | this action opens flow `X` (or merges scratch data into it) |
| `requiresFlow=X` | refuse unless `currentFlow.name === X` |
| `endsFlow` | clears `currentFlow` + `awaitingInput` on `ok` |
| `issuesOtp→consumer` | this action mints an SCA challenge; library sets `awaitingInput=otp` for `consumer` |
| `requiresOtp` | refuse unless `awaitingInput.kind="otp"` for this action; auto-clears on `ok` |
| `startsMatchFor→consumer` | double-entry capturer; library sets `awaitingInput=match` for `consumer` |
| `requiresMatch(capturer, N)` | double-entry consumer; library decrements on `verdict: "match_mismatch"`, aborts on exhaustion |
| `soleStep` / `soleOnExecute` | batch-shape constraint (strict / relaxed-for-propose) |

## Verifiers (one file per prereq)

| name | predicate | denial |
|------|-----------|--------|
| {{prereqName_1}} | `state.X != null` | `{ summary: "...", error: "X_not_verified" }` |
| {{prereqName_2}} | `state.Y != null && state.Z[state.Y] != null` | `{ summary: "...", error: "Y_not_verified" }` |

## State slot additions (src/state.ts)

- `verified{{Entity}}` : `Record<string, Verified{{Entity}}>` — record-by-key merge
- `active{{Entity}}{{Id}}` : `string | null` — replace-on-write
- `awaitingInput` : library-managed; already scaffolded in `state.ts`
- `currentFlow` : library-managed; already scaffolded in `state.ts`

## Multi-turn flows (if any)

| flow | opened by | closed by | scratch data shape (currentFlow.data) |
|------|-----------|-----------|----------------------------------------|
| {{flow_1}} | {{request_action}} (startsFlow) | {{commit_action}} (endsFlow) or an `abort_flow` effect | `{ customerId, challengeId, otpValidated?, encryptedPin? ... }` |

## Backend env vars (per-tool, in backend/env.ts)

| field | env var | required |
|-------|---------|----------|
| {{field_1}} | `{{ENV_1}}` | yes |
| {{field_2}} | `{{ENV_2}}` | yes |

Plus shared envelope fields: `applicationId`, `bank`, `channel`, `userId`, `branchCode`, `workstation`, `sandboxId`.

## Backend endpoints

| action | endpoint | method | envelope | notes |
|--------|----------|--------|----------|-------|
| {{action_1}} | `{{ENDPOINT_1}}` | POST | full | {{notes}} |
| {{action_3}} (pre-read) | `{{ENDPOINT_READ}}` | POST | payload-only | internal to executor |
| {{action_3}} (write) | `{{ENDPOINT_WRITE}}` | POST | payload-only | internal to executor |
| {{action_3}} (post-read) | `{{ENDPOINT_READ}}` | POST | payload-only | internal to executor |

## Prompt edits (src/prompt.ts)

- **SCOPE** — {{if scope changes}}: {{describe broaden/narrow}}
- **ACTIONS** — append {{TOOL_NAME}} block(s) listing each action's params/prereqs/result body
- **MUTATION SAFETY** — {{if confirm-required mutations exist}}: add propose/execute/lockdown/recap rules for `{{mutation_action}}`; mention `abort_pending_input` as the universal abort
- **FLOW NARRATIVE** — {{if multi-turn flows exist}}: explain the per-turn shape (open → OTP → next step → commit), how OTP recovery works (timeout vs lock vs wrong), and how double-entry match works if applicable
- **EXAMPLES** — add 1-2 worked batches showing the read flow, the propose+execute flow, and (if applicable) one multi-turn flow turn

## tools/index.ts

- `import { {{TOOL_NAME}}Tool } from "./{{TOOL_NAME}}/index.js";`
- Add `{{TOOL_NAME}}Tool` to the `tools` array

## Open questions

- {{any backend response shape that wasn't documented?}}
- {{any verdict the executor should return that wasn't in the spec?}}
- {{any state-merge semantics to confirm?}}

## Risks & gotchas

- {{e.g. "two actions both call the same endpoint with different params — fine, but worth flagging"}}
- {{e.g. "prereq X requires state slot Y to be populated by action Z — execution order in batch matters"}}

---

**Decision:** approve / request changes / discuss.
