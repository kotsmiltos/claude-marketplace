{
  "name": "kafka-observability",
  "version": "0.2.0",
  "description": "LangSmith-parity observability over Kafka for any LangGraph.js / LangChain.js agent. Ships a vendored src/observability/ library — a BaseTracer subclass that captures EVERY traced run (graph, nodes, LLM calls with full prompts/outputs/token usage, tool runs, errors, trace hierarchy) exactly as LangSmith receives it, and publishes each run as start/end events to a Kafka topic over a bounded, non-blocking, fire-and-forget producer (librdkafka, acks=all, idempotent). Disabled by default (KAFKA_ENABLED). The /add-kafka-observability skill installs OR upgrades the library in a target repo: vendors the module, adds the client dependency, wires one startup() call at the graph entrypoint (global callback hook — no per-node instrumentation), updates .env.example and deployment settings, and verifies with typecheck + unit tests. Intake proposes broker/topic/security defaults discovered from sibling repos in the workspace that already carry the library (propose-only; the user confirms every value). Runs alongside LangSmith so it can be validated in parallel before LangSmith is unplugged.",
  "author": {
    "name": "Charalampos Kyfonidis"
  },
  "keywords": ["kafka", "observability", "langsmith", "tracing", "langgraph", "langchain", "audit"]
}
